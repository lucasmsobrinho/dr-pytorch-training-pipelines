{
    "name": "VGG_Jabbar_CyclicLR_SGD_no_earlystop_naive",
    "n_gpu": 1,

    "arch": {
        "type": "VGG_Jabbar",
        "args": {
            "num_classes": 5,
            "freeze_cnn": false,
	        "pretrained": true
        }
    },

    "train_set": {
        "type": "EyePacsDataset",
        "args": {
            "annotations_file": "../scratch/train_aug_labels_naive.csv",
            "img_dir": "../scratch/train256/"
        }
    },
    "valid_set": {
        "type": "EyePacsDataset",
        "args": {
            "annotations_file": "../scratch/valid_aug_labels_naive.csv",
            "img_dir": "../scratch/train256/"
        }
    },
    "test_set": {
        "type": "EyePacsDataset",
        "args": {
            "annotations_file": "../scratch/test_labels.csv",
            "img_dir": "../scratch/test256/"
        }
    },

    "data_loader": {
        "type": "EyePacsDataLoader",
        "args":{
            "batch_size": 64,
            "shuffle": true,
            "num_workers": 4
        }
    },

    "optimizer": {
        "type": "SGD",
        "args":{
            "lr": 0.01
        }
    },
    "loss": "categorical_cross_entropy_loss",
    "metrics": [
        "accuracy"
    ],
    "lr_scheduler": {
        "type": "CyclicLR",
        "args": {
            "base_lr": 0.0001,
            "max_lr": 0.01,
            "step_size_up": 20000,
            "step_size_down": 20000
        }
    },
    "trainer": {
        "epochs": 150,

        "save_dir": "saved/",
        "save_period": 5, 
        "verbosity": 2,

        "monitor": "min val_loss",
        "early_stop": 150,

        "tensorboard": true
    }
}
